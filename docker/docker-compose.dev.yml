version: '3.3'

services:
  web:
    external_links:
      - core_db
    container_name: ${PROJECTNAME}_dev
    environment:
      VIRTUAL_HOST: ${DEV_HOST}
      LETSENCRYPT_HOST: ${DEV_HOST}

networks:
    default:
      external:
        name: corenetwork
